# About

`ansible-awx-demo` is a sanitized, public-safe Ansible project for learning
cross-platform playbooks, reusable roles, inventory design, and AWX workflows.

It uses documentation-only hosts and contains no production credentials or
private infrastructure data.
