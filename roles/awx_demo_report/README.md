# awx_demo_report role

This read-only example role displays the current inventory host, group
membership, connection type, remote user, and operating-system facts. It is
safe for demonstrations and AWX testing because it does not change hosts.

The play that demonstrates it is
`playbooks/examples/role_demo.yml`.
