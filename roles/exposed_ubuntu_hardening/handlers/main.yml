---
- name: Restart SSH service
  ansible.builtin.service:
    name: "{{ exposed_ubuntu_hardening_ssh_service }}"
    state: restarted
  tags:
    - ssh

- name: Restart Fail2ban
  ansible.builtin.service:
    name: fail2ban
    state: restarted
  tags:
    - fail2ban
